# YouTube_advance_orchestrationl_rag
Built an AI video understanding system using RAG and LangGraph-based agent workflows. It processes transcripts, performs semantic retrieval with FAISS, and dynamically selects context (top-k/full) for accurate answers. Includes multi-LLM integration and structured summaries.
